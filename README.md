Accessibility Toolbar

A simple HTML/JavaScript toolbar to add accessibility features to your website.

🚀 Features

- Toggle high contrast / invert colors to make text and background more readable
- Switch to grayscale mode for users sensitive to color intensity
- Increase font size across the page
- Increase letter spacing for improved readability
- Highlight all links for easier navigation
- Save settings (e.g., using cookies/local storage) so users’ preferences persist across visits
- Keyboard accessible: can be triggered via keyboard shortcut and is operable without a mouse
- Lightweight and dependency-free (pure HTML/JS/CSS)
